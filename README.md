A sample book browser SPA written in AngularJs 1.4

After installing the dependencies with `npm install` and `bower install`, run `grunt serve` to develop, or `grunt build` to create a minified distributable.
